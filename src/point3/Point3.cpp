
#include "Point3.h"

namespace turbine_core {

    namespace points {

//        void Point3::updatePoint( const Point3 & reference, const Matrix3<real_t> & fullRotationMatrix, const Vector3<real_t> & relativeRotationalVelocity ) {
//
//            orientation = fullRotationMatrix * relativeOrientation * reference.orientation;
//            position = reference.position + reference.orientation.getTranspose() * relativePosition;
//
//            velocity = reference.velocity + (reference.orientation.getTranspose() * relativePosition) % reference.rotationalVelocity;
//
//            rotationalVelocity = reference.rotationalVelocity + orientation.getTranspose() * relativeRotationalVelocity;
//
//        }

    }

}
